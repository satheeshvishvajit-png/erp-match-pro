Put 20-30 sample invoice PDFs/images here for testing (per the assignment's
"Test Data" step). Search "sample invoice PDF" to find examples, or use the
"Paste invoice text" tab on the Upload page to test field extraction without
needing image files at all.

For each sample invoice you add here, create a matching Purchase Order and
GRN with the same PO Number, Vendor, and Price in modules/odoo/mock_data.py
(or load them into a real Odoo instance and switch ODOO_MODE=live) so the
app has something to match against.
